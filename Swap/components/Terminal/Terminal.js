/**
 * Terminal
 */

/*** Inject HTML ***/

(() => {

    elementify('Main').insertAdjacentHTML('beforeend', `<div id="Terminal" class="card card-swap"><div class="container" id="terminal-container"></div></div>`);

})();

